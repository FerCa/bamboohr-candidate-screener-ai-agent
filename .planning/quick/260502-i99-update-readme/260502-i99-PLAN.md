---
quick_id: 260502-i99
slug: update-readme
description: "Update README — new install.sh setup path, fix env var docs, cron section, dry-run section"
date: 2026-05-02
status: planned
must_haves:
  truths:
    - install.sh is the primary setup path (Docker check + .env validation + image build + cron at 11am)
    - OPENAI_API_KEY is required at startup always (CR-03), not only in live runs
    - CONFIG_PATH default resolves to ./config.yaml = /app/config.yaml inside the container
    - install.sh uses .env in the project root, not /etc/screener.env
    - Cron registered by install.sh runs at 11:00 AM daily
    - needsReview is a valid runtime outcome (CV extraction failures, MaxTurnsExceeded)
    - Summary JSON is on stdout; diagnostic logs are on stderr
  artifacts:
    - README.md updated
---

# Quick Plan: Update README

## Task 1 — Quick Start: add install.sh path

**Files:** `README.md`

**Action:**
Replace the current manual 4-step Quick Start with install.sh as the primary path.
Steps: (1) `cp .env.example .env` + fill in creds, (2) `./install.sh`.
Install.sh does: Docker check, .env validation, image build, cron registration at 11:00 AM.
Keep a one-liner for manual runs after setup.

**Verify:** Quick Start shows `./install.sh` prominently; `.env` (not `/etc/screener.env`) is the env file.

**Done:** Quick Start section updated.

## Task 2 — Fix OPENAI_API_KEY and CONFIG_PATH in env vars table

**Files:** `README.md`

**Action:**
- OPENAI_API_KEY: change Required from "yes (live runs)" to "yes" — the code validates it unconditionally at startup.
- CONFIG_PATH: rewrite description — default `./config.yaml` resolves to `/app/config.yaml` inside the container; only set this if mounting to a non-standard path.

**Verify:** Table row for OPENAI_API_KEY says "yes". CONFIG_PATH description no longer says "Set to /app/config.yaml when mounting via -v".

**Done:** Env vars table corrected.

## Task 3 — Dry-run section: remove CR-01 reference

**Files:** `README.md`

**Action:**
Remove "CR-01 —" internal code-review reference from the SKIPS OpenAI line. Keep the behavior description.

**Verify:** Dry-run bullet no longer mentions "CR-01".

**Done:** Internal reference removed.

## Task 4 — Cron Setup: add install.sh as recommended approach

**Files:** `README.md`

**Action:**
Add an intro paragraph at the top of the Cron Setup section noting that install.sh handles cron registration automatically (11:00 AM daily). The manual macOS/Linux steps that follow are for custom schedules or when you can't use install.sh. Also note install.sh doesn't add log redirection — users who want `/var/log/screener.log` should add `>> /var/log/screener.log 2>&1` to the cron entry manually.

**Verify:** Cron Setup section starts with install.sh recommendation before the macOS/Linux manual steps.

**Done:** Cron Setup section updated.
