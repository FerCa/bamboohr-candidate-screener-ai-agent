---
quick_id: 260502-i99
slug: update-readme
description: "Update README — new install.sh setup path, fix env var docs, cron section, dry-run section"
date: 2026-05-02
status: complete
---

# Summary: Update README (260502-i99)

## What was done

Updated README.md to reflect the current state of the project after all 4 phases are complete.

**Task 1 — Quick Start rewritten around install.sh**
- Replaced 4-step manual setup (build image → copy to /etc/screener.env → docker run → manual cron) with 2 steps: `cp .env.example .env` + `./install.sh`
- Explained what install.sh does (Docker check, env validation, image build, cron at 11:00 AM)
- Added a manual one-liner for ad-hoc runs using `.env` in the project root
- Replaced the old `/etc/screener.env` path with `.env` throughout Quick Start

**Task 2 — Env vars table corrected**
- `OPENAI_API_KEY`: changed "yes (live runs)" → "yes" — the code validates it unconditionally at startup (all modes)
- `CONFIG_PATH`: rewrote description to explain that the default `./config.yaml` resolves to `/app/config.yaml` inside the container; no need to set it when using the standard volume mount

**Task 3 — Internal reference removed from dry-run section**
- Removed "CR-01 —" code-review identifier from the OpenAI skip bullet

**Task 4 — Cron Setup restructured**
- Added "Recommended: use install.sh" subsection at the top explaining the automated 11:00 AM schedule and noting log redirection is not added automatically
- Renamed manual macOS and Linux sections to "Manual setup — macOS / Linux" to clarify they are the fallback path

**Additional cleanup (found during review)**
- Removed `(Phase 2)` from the dry-run CV download bullet
- Added `install.sh` to the Project Structure section
- Removed `(Phase 4)` from Dockerfile and .dockerignore descriptions
- Fixed `.env.example` description: "Template for /etc/screener.env" → "Credentials template — copy to .env and fill in values"

## Files changed

- `README.md`

## Verified

- Quick Start now leads with install.sh and uses `.env` (not `/etc/screener.env`)
- OPENAI_API_KEY shows "yes" as required
- CONFIG_PATH description no longer implies manual override is needed for standard mounts
- No "CR-01" references in user-facing docs
- Cron Setup section has install.sh at the top
- install.sh present in Project Structure
