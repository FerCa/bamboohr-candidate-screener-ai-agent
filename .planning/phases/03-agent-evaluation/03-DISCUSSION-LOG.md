# Phase 3: Agent Evaluation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-02
**Phase:** 3-Agent Evaluation
**Areas discussed:** Soft rules YAML shape, Recruiter comment format, Evaluation output type

---

## Soft rules YAML shape

### Q1 — Structure of each soft rule entry

| Option | Description | Selected |
|--------|-------------|----------|
| Label + description | Each rule has a label and description. Mirrors hard-rule style. | |
| Label + description + required flag | Split into required (dealbreakers) and optional (nice-to-haves). | ✓ |

**User's choice:** Label + description + required flag
**Notes:** User selected the split `required`/`optional` structure. Required = all must pass. Optional = evaluated and reported but non-blocking. Example YAML confirmed via preview.

---

### Q2 — Pass logic for soft rules

| Option | Description | Selected |
|--------|-------------|----------|
| All required must pass | All required criteria must pass. Optional never block. | ✓ |
| Min-count threshold | Configurable minimum number of required criteria must pass. | |

**User's choice:** All required must pass
**Notes:** Cleanest approach — consistent with Phase 1 hard rules (collect-all, all must pass).

---

### Q3 — GPT-4o output per criterion

| Option | Description | Selected |
|--------|-------------|----------|
| Pass/fail + one-line rationale | Boolean met + evidence string per criterion. Used in log and comment. | ✓ |
| Pass/fail only | Boolean only. Simpler but comment has less grounding. | |
| Score + rationale | Numeric confidence + rationale. Adds threshold complexity. | |

**User's choice:** Pass/fail + one-line rationale
**Notes:** Rationale is GPT-4o generated, grounded in actual candidate content.

---

### Q4 — Optional criteria in output

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — include optional results | Optional criteria included in log JSON and recruiter comment. | ✓ |
| No — required criteria only | Optional evaluated internally, not surfaced. | |

**User's choice:** Yes — include optional results
**Notes:** Gives recruiters full picture. Optional section appears in both stdout JSON and BambooHR comment.

---

## Recruiter comment format

### Q1 — Comment structure

| Option | Description | Selected |
|--------|-------------|----------|
| Structured list | Fixed sections: outcome header, bullet lists for met/unmet/optional criteria. | ✓ |
| Narrative paragraph | 3–5 sentence paragraph. More natural but harder to scan. | |

**User's choice:** Structured list
**Notes:** Fixed format confirmed via preview. Sections: outcome header, Met (required), Unmet (required), Optional, footer `[Auto-screened by AI — final decision rests with recruiter]`.

---

### Q2 — Comment scope

| Option | Description | Selected |
|--------|-------------|----------|
| Soft evaluation only | Hard-rule pass is implicit. Comment covers GPT-4o criteria only. | ✓ |
| Hard rules summary too | Also lists which hard rules passed. Redundant for pass candidates. | |

**User's choice:** Soft evaluation only
**Notes:** If a candidate reached soft evaluation, hard rules already passed — no need to repeat them.

---

### Q3 — Per-criterion rationale source

| Option | Description | Selected |
|--------|-------------|----------|
| GPT-4o generates rationale | Evidence string generated by GPT-4o, grounded in actual candidate content. | ✓ |
| Fixed rule description | Rationale is the rule's `description` field — same for all candidates. | |

**User's choice:** GPT-4o generates rationale
**Notes:** Rationale should cite actual CV/answer content, not the generic rule description.

---

## Evaluation output type

### Q1 — Type structure

| Option | Description | Selected |
|--------|-------------|----------|
| New EvaluationResult type | New `src/agent/types.ts` with per-criterion breakdown + comment. Clean separation. | ✓ |
| Extend CandidateDecision | Add optional fields to existing type. One type across all phases. | |

**User's choice:** New EvaluationResult type
**Notes:** `EvaluationResult` in `src/agent/types.ts`. Fields: applicationId, applicantId, outcome, required[], optional[], comment, timestamp.

---

### Q2 — Stdout format for evaluated candidates

| Option | Description | Selected |
|--------|-------------|----------|
| EvaluationResult to stdout | EvaluationResult logged directly for soft-evaluated candidates. Richer output. | ✓ |
| CandidateDecision stays as stdout | EvaluationResult internal only; CandidateDecision used for all paths. | |

**User's choice:** EvaluationResult to stdout
**Notes:** Hard-rule fails, PDF needsReview, and errors continue to use CandidateDecision. Only soft-evaluated candidates (pass/fail/needsReview from GPT-4o) use EvaluationResult as the log record.

---

## Claude's Discretion

- **Agent architecture:** No tools needed — all data is in CandidateContext. SDK used for Runner.run() / maxTurns loop with structured output. Single-turn call; maxTurns ≤ 5 trivially satisfied. Not discussed by user — left to implementation.
- **File layout:** `src/agent/` directory (types.ts, evaluator.ts, prompt.ts). Internal split up to Claude.
- **Log function:** Whether to add `logEvaluation()` or overload `logDecision()` for EvaluationResult — Claude's discretion.
- **Zod schema reuse:** EvaluationResult Zod schema should double as the GPT-4o structured output schema to avoid drift between types.

## Deferred Ideas

None — discussion stayed within phase scope.
