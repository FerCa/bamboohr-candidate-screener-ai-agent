---
phase: 06-multi-job-refactor
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/pipeline/candidate-processor.ts
  - src/rules/evaluator.ts
autonomous: true
requirements:
  - MULTI-01

must_haves:
  truths:
    - "CandidateProcessor constructor accepts JobConfig instead of full Config"
    - "evaluateHardRules accepts JobConfig instead of full Config"
    - "CandidateProcessor uses job.stages.fail and job.stages.pass (not config.job.stages.*)"
    - "evaluateHardRules destructures job.hardRules and job.fieldMap (not config.hardRules and config.fieldMap)"
    - "No `config.job.*` references remain in candidate-processor.ts or evaluator.ts"
  artifacts:
    - path: "src/pipeline/candidate-processor.ts"
      provides: "CandidateProcessor accepting JobConfig"
      contains: "JobConfig"
    - path: "src/rules/evaluator.ts"
      provides: "evaluateHardRules(job: JobConfig, application)"
      contains: "JobConfig"
  key_links:
    - from: "src/pipeline/candidate-processor.ts"
      to: "src/rules/evaluator.ts"
      via: "evaluateHardRules(this.job, applicationDetail)"
      pattern: "evaluateHardRules"
    - from: "src/pipeline/candidate-processor.ts"
      to: "src/config/schema.ts"
      via: "import type { JobConfig }"
      pattern: "JobConfig"
---

<objective>
Update `CandidateProcessor` and `evaluateHardRules` to accept a `JobConfig` slice instead of the full `Config` object. These two files both access `config.job.*`, `config.hardRules`, and `config.fieldMap` — all of which move inside the per-job entry after Plan 01's schema change.

Purpose: After this plan, `CandidateProcessor` and `evaluateHardRules` are fully decoupled from the full multi-job `Config` type. They receive only the single-job slice they need, which is the clean DI pattern established in Phase 5.

Output: Updated `src/pipeline/candidate-processor.ts` and `src/rules/evaluator.ts` with `JobConfig` parameter replacing `Config`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-multi-job-refactor/06-CONTEXT.md
@.planning/phases/06-multi-job-refactor/06-PATTERNS.md
@.planning/phases/06-multi-job-refactor/06-01-SUMMARY.md

<interfaces>
<!-- evaluateHardRules current signature (to change): -->
```typescript
// Current:
export function evaluateHardRules(config: Config, application: BambooHRApplication): RuleResult {
  const { hardRules, fieldMap } = config;
  // ...
}

// New:
export function evaluateHardRules(job: JobConfig, application: BambooHRApplication): RuleResult {
  const { hardRules, fieldMap } = job;
  // ...
}
```

<!-- CandidateProcessor current config references (to change): -->
```typescript
// Current constructor:
private readonly config: Config

// New constructor:
private readonly job: JobConfig

// References inside process() that change:
this.config.job.stages.fail   → this.job.stages.fail
this.config.job.stages.pass   → this.job.stages.pass
evaluateHardRules(this.config, applicationDetail) → evaluateHardRules(this.job, applicationDetail)
this.config.softRules         → this.job.softRules   (if referenced)
```

<!-- JobConfig shape (from Plan 01 schema.ts): -->
```typescript
export type JobConfig = {
  openingId: string;
  stages: { intake: string; pass: string; fail: string };
  hardRules: { maxSalary?: ...; requiredFields?: ...; requiredBoolean?: ...; requiredKeyword?: ... };
  fieldMap: Record<string, string>;
  softRules?: { required: ...[]; optional: ...[] };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update evaluateHardRules signature from Config to JobConfig</name>
  <files>src/rules/evaluator.ts</files>
  <read_first>
    - src/rules/evaluator.ts (read the FULL file — understand what fields are destructured from config; confirm only `hardRules` and `fieldMap` are used, not `config.job.*`)
    - src/config/schema.ts (confirm JobConfig export name after Plan 01)
  </read_first>
  <action>
In `src/rules/evaluator.ts`:

1. Change the import at the top of the file:
   - Remove: `import type { Config } from '../config/schema.js';`
   - Add: `import type { JobConfig } from '../config/schema.js';`

2. Change the `evaluateHardRules` function signature:
   - From: `export function evaluateHardRules(config: Config, application: BambooHRApplication): RuleResult {`
   - To: `export function evaluateHardRules(job: JobConfig, application: BambooHRApplication): RuleResult {`

3. Change the destructure line inside the function body:
   - From: `const { hardRules, fieldMap } = config;`
   - To: `const { hardRules, fieldMap } = job;`

ALL other logic inside `evaluateHardRules` is UNCHANGED. The function accesses only `hardRules` and `fieldMap` — both are present at the top level of `JobConfig` (same as they were at the top level of the old flat `Config`). No other changes needed.

Do NOT change any helper functions (`resolveField`, etc.) — those take explicit parameters and are unaffected.
  </action>
  <verify>
    <automated>grep -c "JobConfig" /Users/ferrancaellas/projects/bamboohr-candidate-screener-ai-agent/src/rules/evaluator.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "JobConfig" src/rules/evaluator.ts` returns a match
    - `grep "evaluateHardRules(job: JobConfig" src/rules/evaluator.ts` returns a match
    - `grep "const { hardRules, fieldMap } = job" src/rules/evaluator.ts` returns a match
    - `grep "import type { Config }" src/rules/evaluator.ts` returns no match (old import removed)
    - `grep "config\.job\." src/rules/evaluator.ts` returns no match
  </acceptance_criteria>
  <done>evaluateHardRules accepts JobConfig, destructures hardRules and fieldMap from job (not config); all rule evaluation logic unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Update CandidateProcessor constructor from Config to JobConfig</name>
  <files>src/pipeline/candidate-processor.ts</files>
  <read_first>
    - src/pipeline/candidate-processor.ts (read the FULL file — find every reference to `this.config.job.*` and `this.config.softRules`; also note the evaluateHardRules call site)
    - src/rules/evaluator.ts (confirm the updated evaluateHardRules signature accepts JobConfig — needed to update the call site in candidate-processor.ts)
  </read_first>
  <action>
In `src/pipeline/candidate-processor.ts`:

1. Change the import:
   - Remove: `import type { Config } from '../config/schema.js';`
   - Add: `import type { JobConfig } from '../config/schema.js';`

2. Change the constructor private field:
   - From: `private readonly config: Config,`
   - To: `private readonly job: JobConfig,`

3. Update ALL references to `this.config` inside the `process()` method:
   - `evaluateHardRules(this.config, applicationDetail)` → `evaluateHardRules(this.job, applicationDetail)`
   - `this.config.job.stages.fail` → `this.job.stages.fail`
   - `this.config.job.stages.pass` → `this.job.stages.pass`
   - `this.config.softRules` → `this.job.softRules` (if referenced — check the full file)

4. If there is a `resolveStageId` private method that takes `this.config` or `this.config.job`, update those references too.

Do NOT change any other logic. The per-candidate pipeline behavior is UNCHANGED — only the config access path changes from `this.config.job.*` → `this.job.*` and `this.config.*` → `this.job.*`.

After this change, callers of `new CandidateProcessor(...)` must pass a `JobConfig` slice as the 5th argument (previously full `Config`). The caller in `src/__tests__/ScreeningPipeline.test.ts` will be updated in Plan 05. The caller in `MultiJobOrchestrator` (Plan 04) will use the correct shape from day one.
  </action>
  <verify>
    <automated>grep -c "JobConfig" /Users/ferrancaellas/projects/bamboohr-candidate-screener-ai-agent/src/pipeline/candidate-processor.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "JobConfig" src/pipeline/candidate-processor.ts` returns a match
    - `grep "private readonly job: JobConfig" src/pipeline/candidate-processor.ts` returns a match
    - `grep "this\.job\." src/pipeline/candidate-processor.ts` returns at least 3 matches (stages.fail, stages.pass, softRules or hardRules)
    - `grep "this\.config" src/pipeline/candidate-processor.ts` returns no match (all config references replaced with job)
    - `grep "config\.job\." src/pipeline/candidate-processor.ts` returns no match
    - `grep "evaluateHardRules(this\.job" src/pipeline/candidate-processor.ts` returns a match
  </acceptance_criteria>
  <done>CandidateProcessor constructor accepts JobConfig; all internal references to config.job.* replaced with job.*; evaluateHardRules call site updated to pass this.job</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| evaluateHardRules receives wrong job slice (e.g., job-2's hardRules applied to job-1 candidates) | HIGH | JobRunner (Plan 02) passes `this.job` to CandidateProcessor constructor, and CandidateProcessor passes `this.job` to evaluateHardRules — the slice is fixed at construction time per job; no index arithmetic in the evaluation path |
| CandidateProcessor.process() still references `this.config.*` after edit (partial refactor) | HIGH | Acceptance criteria includes `grep "this\.config" src/pipeline/candidate-processor.ts` returns no match — compiler and grep gate catch any missed reference |
| softRules from wrong job applied to candidates (GPT-4o evaluates against wrong criteria) | HIGH | CandidateProcessor receives `JobConfig` slice at construction; `this.job.softRules` is the per-job soft rule set — MultiJobOrchestrator constructs a fresh CandidateProcessor per job (Plan 04), so no cross-job contamination |
| evaluateHardRules signature change breaks existing unit tests | MEDIUM | Existing unit tests for evaluateHardRules pass a `Config` object — after Plan 01, the shape changed. Tests in `src/__tests__/` that call `evaluateHardRules` directly must be updated in Plan 05. Compile errors will surface this clearly. |
| Lock file race (SAFE-03 deferred) | MEDIUM | Accepted per CONTEXT.md deferred section |
</threat_model>

<verification>
After both tasks complete:
- `grep "JobConfig" src/rules/evaluator.ts` — returns match
- `grep "JobConfig" src/pipeline/candidate-processor.ts` — returns match
- `grep "config\.job\." src/rules/evaluator.ts` — returns no match
- `grep "config\.job\." src/pipeline/candidate-processor.ts` — returns no match
- `grep "this\.config" src/pipeline/candidate-processor.ts` — returns no match
- `npx tsc --noEmit` — errors should only be in files not yet updated (screening-pipeline.ts, index.ts, test files — fixed in Plans 05)
</verification>

<success_criteria>
- `evaluateHardRules(job: JobConfig, application)` signature in evaluator.ts
- `CandidateProcessor` constructor field is `job: JobConfig` (not `config: Config`)
- Zero `config.job.*` or `this.config.*` references in either file
- Both files compile without errors (type-check passes for these two files)
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-job-refactor/06-03-SUMMARY.md`
</output>
