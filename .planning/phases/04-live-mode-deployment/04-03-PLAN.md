---
phase: 04-live-mode-deployment
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - README.md
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "A README.md file exists at the project root"
    - "README contains a copy-paste-ready macOS crontab line that runs the docker container daily"
    - "The crontab entry uses --env-file (D-06) and -v config volume mount (D-07) — not inline -e flags"
    - "README includes a Linux deployment note (D-08) explaining the same docker run command works on any Linux host with Docker installed"
    - "README documents the required env-file contents (BAMBOOHR_API_KEY, BAMBOOHR_SUBDOMAIN, OPENAI_API_KEY, LIVE_MODE, CONFIG_PATH)"
    - "README documents that the BambooHR API key requires ATS settings access for the write endpoints"
    - "README documents that LIVE_MODE=true requires a signed DPA with OpenAI and candidate consent disclosure (compliance pre-requisite, per REQUIREMENTS.md)"
    - "README documents how to read the INFRA-03 JSON summary line from cron logs to detect successful runs"
  artifacts:
    - path: "README.md"
      provides: "Top-level project documentation including build, run, and cron sections"
      contains: "## Cron Setup"
      min_lines: 80
  key_links:
    - from: "README cron entry"
      to: "Dockerfile image name (bamboohr-screener:latest)"
      via: "literal docker run command"
      pattern: "bamboohr-screener:latest"
    - from: "README env-file template"
      to: ".env.example file in repo"
      via: "documented as the source-of-truth template"
      pattern: "\\.env\\.example"
---

<objective>
Document the deployment story so an operator can go from a fresh clone to a daily-running cron entry without reading research artifacts. The README covers Docker build, the runtime contract (env-file + config volume), the macOS crontab entry (per D-06/D-08), a Linux deployment note (per D-08), the LIVE_MODE compliance gate, and how to read the INFRA-03 summary line from cron logs.

Purpose: INFRA-04 explicitly requires "a copy-paste-ready macOS crontab entry and a note on deploying the same command to a Linux server." Without documentation, the deliverables in Plans 1 and 2 are invisible to anyone who didn't write them.
Output: A new `README.md` at the project root with sections for Overview, Quick Start, Build, Run (dry-run + LIVE_MODE), Configuration, Cron Setup (macOS + Linux), Operating Notes, and Compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-live-mode-deployment/04-CONTEXT.md
@.planning/phases/04-live-mode-deployment/04-RESEARCH.md
@.planning/phases/04-live-mode-deployment/04-PATTERNS.md

@.env.example
@CLAUDE.md
@Dockerfile
@.dockerignore

<interfaces>
<!-- Runtime contract the README must document accurately. -->

Image name (from Plan 02 build command): `bamboohr-screener:latest`

Required env vars (sourced from `src/index.ts` and `.env.example`):
- `BAMBOOHR_API_KEY` — read by src/index.ts:27 (no default, exits 1 if missing)
- `BAMBOOHR_SUBDOMAIN` — read by src/index.ts:28 (no default, exits 1 if missing)
- `OPENAI_API_KEY` — used by @openai/agents SDK when `evaluateSoftRules` runs
- `LIVE_MODE` — must equal exactly the literal string `"true"` to enable BambooHR writes (loader.ts:39)
- `CONFIG_PATH` — defaults to `./config.yaml`; should be set to `/app/config.yaml` inside the container

Container working directory: `/app` (set by Dockerfile WORKDIR)
Container entrypoint: `node dist/index.js` (no args, exec form)
Config mount target inside container: `/app/config.yaml`
Exit codes: 0 on success, 1 on fatal error (existing main().catch(... process.exit(1)))

INFRA-03 final stdout line shape (from Plan 01):
```json
{"processed":N,"pass":N,"fail":N,"needsReview":N,"errors":N}
```

Phase compliance gate (REQUIREMENTS.md Compliance Note):
> Before enabling `LIVE_MODE=true` against real candidates: (1) signed DPA with OpenAI, (2) candidate consent disclosure on application form. Cannot be resolved in code.

</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write README.md with build, run, configuration, and cron sections (INFRA-04)</name>
  <files>README.md</files>
  <read_first>
    - .env.example (verify variable names match exactly: BAMBOOHR_API_KEY, BAMBOOHR_SUBDOMAIN, OPENAI_API_KEY, LIVE_MODE, CONFIG_PATH)
    - CLAUDE.md (constraints: applicationId, LIVE_MODE opt-in, dry-run default)
    - Dockerfile (confirm image entrypoint, working dir, user — README must match what was actually built)
    - .dockerignore (confirm what is and isn't in the image)
    - .planning/REQUIREMENTS.md (Compliance Note section for LIVE_MODE pre-requisites)
    - .planning/phases/04-live-mode-deployment/04-CONTEXT.md (D-06, D-07, D-08)
    - .planning/phases/04-live-mode-deployment/04-RESEARCH.md (Pattern 8: Crontab Entry section — copy paste-ready snippet)
  </read_first>
  <action>
    Create a new file `README.md` at the project root. Write the content below verbatim — this is the README that an operator will use. Do NOT shorten, do NOT add a "TBD" or "future" marker. Every section listed below MUST be present and populated.

    ```markdown
    # BambooHR Candidate Screener

    Automated daily screening agent that monitors a BambooHR job opening for new candidates,
    evaluates them against YAML-defined rules (hard rules deterministically, soft rules via
    GPT-4o), and moves them to the configured pass/fail pipeline stages with a structured
    recruiter comment. Runs as a short-lived Docker container triggered by `cron`.

    **Stack:** TypeScript 5 / Node.js 22 LTS · `@openai/agents` · `pdf-parse` · `js-yaml` + `zod` · `node:22-alpine` Docker image.

    **Default mode:** dry-run. The container makes zero BambooHR writes and zero OpenAI calls
    unless `LIVE_MODE=true` is explicitly set.

    ---

    ## Quick Start

    ```bash
    # 1. Build the image
    docker build -t bamboohr-screener:latest .

    # 2. Copy the env template and fill in real values
    cp .env.example /etc/screener.env
    # then edit /etc/screener.env with your real BAMBOOHR_API_KEY, BAMBOOHR_SUBDOMAIN, OPENAI_API_KEY

    # 3. Dry-run against the live BambooHR API (still no writes — LIVE_MODE is unset by default)
    docker run --rm \
      --env-file /etc/screener.env \
      -v "$(pwd)/config.yaml:/app/config.yaml:ro" \
      bamboohr-screener:latest

    # 4. When ready to go live, set LIVE_MODE=true in /etc/screener.env (see Compliance below first)
    ```

    The final stdout line of every run is a JSON summary:

    ```json
    {"processed":3,"pass":1,"fail":1,"needsReview":1,"errors":0}
    ```

    ---

    ## Build

    The image is a multi-stage `node:22-alpine` build (see `Dockerfile`). Stage 1 installs all
    dependencies and runs `tsc`; stage 2 ships only `dist/` and runtime deps.

    ```bash
    docker build -t bamboohr-screener:latest .
    ```

    The image runs as a non-root user (`screener`) with `ENTRYPOINT ["node", "dist/index.js"]`
    in exec form so the process is PID 1 and receives `SIGTERM` cleanly from `docker stop`.

    ## Run

    ### Required environment variables

    | Variable | Required | Default | Purpose |
    |---|---|---|---|
    | `BAMBOOHR_API_KEY` | yes | — | BambooHR Basic-auth username. Must have ATS settings access for the write endpoints (`postComment`, `moveStage`). Exits 1 if unset. |
    | `BAMBOOHR_SUBDOMAIN` | yes | — | Your BambooHR company subdomain (e.g. `acme` for `acme.bamboohr.com`). Exits 1 if unset. |
    | `OPENAI_API_KEY` | yes (live runs) | — | Used by `@openai/agents` for soft-rule evaluation. Not consulted in dry-run (CR-01 fix). |
    | `LIVE_MODE` | no | unset | Set to the literal string `true` to enable BambooHR writes AND OpenAI calls. Any other value (including absent) keeps dry-run. |
    | `CONFIG_PATH` | no | `./config.yaml` | Path inside the container. Set to `/app/config.yaml` when mounting via `-v`. |

    Secrets are passed via `--env-file` at `docker run` time (see [Cron Setup](#cron-setup)) —
    NEVER baked into the image with `ENV` directives, NEVER passed inline as `-e KEY=value` in
    cron entries (which would expose them in shell history and cron logs).

    ### Required volume mount

    The YAML rules file is mounted into the container at runtime:

    ```bash
    -v /absolute/path/to/config.yaml:/app/config.yaml:ro
    ```

    The `:ro` flag makes the mount read-only — the container never writes to the config.

    ### Dry-run vs LIVE_MODE

    **Dry-run** (default — `LIVE_MODE` unset or any value other than `"true"`):
    - Fetches candidates from BambooHR
    - Evaluates hard rules deterministically
    - Downloads CV PDFs and extracts text (Phase 2)
    - SKIPS the OpenAI soft-rule evaluation (CR-01 — synthesizes a placeholder result)
    - SKIPS all BambooHR write calls (`postComment`, `moveStage`)
    - Logs every candidate decision as JSON to stdout
    - Emits the INFRA-03 summary line as the final stdout line

    **LIVE_MODE=true:**
    - All of the above, PLUS:
    - Calls `@openai/agents` to evaluate soft rules with GPT-4o for candidates that passed hard rules
    - Posts a recruiter comment on every processed application (D-04 — pass, fail, needsReview)
    - Moves the candidate to the configured pass/fail pipeline stage
    - Hard-rule fails are also moved (to the fail/reviewed stage) with a `FAIL — Hard rules` comment (D-05)
    - Comment is posted BEFORE the stage move; if the comment POST fails, the stage move is NOT attempted (D-03 atomicity)

    ## Configuration

    Edit `config.yaml` in the project root (or your own copy mounted at `/app/config.yaml`).
    See `config.yaml` for the full schema; key sections:

    - `job.openingId` — BambooHR job opening ID this run targets
    - `job.stages.intake` — pipeline stage name fetched from
    - `job.stages.pass` — destination stage for soft-eval pass
    - `job.stages.fail` — destination stage for fail, hard-rule fail, AND needsReview (per D-01)
    - `hardRules` — deterministic pre-filter (salary ceiling, required fields, boolean / keyword)
    - `softRules.required` and `softRules.optional` — GPT-4o evaluation criteria
    - `fieldMap` — maps logical field names to BambooHR API JSON paths

    Stage names in `config.yaml` are validated against the live BambooHR API at startup
    (CONF-02). If any name does not match a live stage, the container exits 1 before any
    candidate is processed.

    ## Cron Setup

    The container is designed to be triggered by an external cron (no internal scheduler).
    Each run processes all currently-`intake`-stage candidates and exits.

    ### macOS

    1. Save your secrets to `/etc/screener.env` (or any path the cron user can read):

       ```bash
       sudo cp .env.example /etc/screener.env
       sudo chmod 600 /etc/screener.env
       sudo $EDITOR /etc/screener.env
       ```

       Required contents (all four MUST be set; uncomment `LIVE_MODE=true` only after the
       compliance gate is cleared — see [Compliance](#compliance)):

       ```
       BAMBOOHR_API_KEY=<your-api-key>
       BAMBOOHR_SUBDOMAIN=<your-subdomain>
       OPENAI_API_KEY=<your-openai-key>
       CONFIG_PATH=/app/config.yaml
       # LIVE_MODE=true
       ```

    2. Place your rules file somewhere stable (e.g. `/etc/screener-config.yaml`) so cron can
       mount the same path daily:

       ```bash
       sudo cp config.yaml /etc/screener-config.yaml
       ```

    3. Install the daily cron entry with `crontab -e` and add this line — copy-paste exactly:

       ```cron
       # Run BambooHR candidate screener daily at 08:00 local time
       0 8 * * * /usr/local/bin/docker run --rm --env-file /etc/screener.env -v /etc/screener-config.yaml:/app/config.yaml:ro bamboohr-screener:latest >> /var/log/screener.log 2>&1
       ```

       Notes:
       - `/usr/local/bin/docker` is the absolute path to Docker on macOS (cron's PATH is minimal — relative `docker` may not resolve). Verify with `which docker` and adjust if your install differs.
       - `>> /var/log/screener.log 2>&1` captures BOTH stdout (JSON log records and the INFRA-03 summary) and stderr (mode banner, diagnostic messages) into one file.
       - `--rm` ensures the container is removed after exit so cron does not accumulate stopped containers.

    4. Verify the next day:

       ```bash
       tail -1 /var/log/screener.log
       # Expected: {"processed":N,"pass":N,"fail":N,"needsReview":N,"errors":N}
       ```

       If the last line of any run is NOT a parseable JSON object with those five keys, the
       run failed before completing the loop. Check the lines above for the error.

    ### Linux server

    The same `docker run` command works on any Linux host with Docker installed. The only
    differences from the macOS instructions:

    - The Docker binary is typically `/usr/bin/docker` (not `/usr/local/bin/docker`). Verify with `which docker`.
    - cron syntax is the same. Use `crontab -e` for a per-user entry, or drop a file in
      `/etc/cron.d/` for a system-wide one (the `/etc/cron.d/screener` file would have a
      `user` field added between the schedule and the command):

      ```cron
      0 8 * * * root /usr/bin/docker run --rm --env-file /etc/screener.env -v /etc/screener-config.yaml:/app/config.yaml:ro bamboohr-screener:latest >> /var/log/screener.log 2>&1
      ```

    - File permissions: `/etc/screener.env` should be `chmod 600` and owned by the user that
      runs the cron job (root for `/etc/cron.d/`).
    - Time zone: cron honors the system time zone. Confirm with `date` before scheduling.

    ## Operating Notes

    - **Idempotency is not yet implemented** (SAFE-03 deferred to v2). Re-running the
      container against a stage that has already been processed will re-comment and re-move
      candidates that are still in the intake stage. In practice the daily cron leaves no
      candidates in `intake` after a successful run, so re-runs are safe — but a manual
      re-trigger between cron runs may double-comment any candidate that was somehow left
      behind.
    - **Health detection:** parse the final line of stdout for the JSON summary
      (`{"processed":...,"errors":N}`). If `errors > 0`, the run had per-candidate failures
      that did not abort the loop (SAFE-01 isolation). If the JSON line is missing entirely,
      the run aborted before completion.
    - **First-run discovery:** if `config.yaml` `fieldMap` contains `REPLACE_WITH...`
      placeholders, the container logs the application detail JSON structure on the first
      candidate so you can fill in real BambooHR field paths.
    - **Logs do not contain raw CV text or candidate PII beyond what the recruiter comment
      already includes.** CV text flows only through memory during processing (GDPR).

    ## Compliance

    > **Before setting `LIVE_MODE=true` against real candidates, the following are required:**
    >
    > 1. A signed Data Processing Agreement (DPA) with OpenAI — required under GDPR because
    >    candidate CVs are sent to OpenAI for soft-rule evaluation.
    > 2. The job application form must disclose to candidates that submitted data may be
    >    processed by AI tools.
    >
    > These are legal requirements, not implementation tasks. They cannot be resolved in code.

    Additionally, the BambooHR API key used must have ATS settings access — without that
    permission level, the `postComment` and `moveStage` write endpoints return HTTP 403 and
    every candidate in a LIVE_MODE run will be counted as an `error`. Verify the key
    permission in BambooHR Settings → API Keys before flipping `LIVE_MODE=true`.

    ## Project Structure

    ```
    .
    ├── src/
    │   ├── index.ts                # Entry point — startup, candidate loop, write guards (Phase 4)
    │   ├── bamboohr/client.ts      # BambooHR REST client — get, post, postComment, moveStage
    │   ├── config/                 # YAML schema (Zod) + fail-fast loader
    │   ├── rules/                  # Hard-rule evaluator + types
    │   ├── pipeline/               # PDF download + text extraction
    │   ├── agent/                  # @openai/agents soft-rule evaluator
    │   └── logger/                 # JSON-line decision/evaluation loggers
    ├── Dockerfile                  # Multi-stage node:22-alpine build (Phase 4)
    ├── .dockerignore               # Build context exclusions (Phase 4)
    ├── config.yaml                 # Rules + stage configuration (operator-edited)
    ├── .env.example                # Template for /etc/screener.env
    └── .planning/                  # GSD planning artifacts (excluded from Docker image)
    ```

    See `CLAUDE.md` for engineering constraints (`applicationId` not `applicantId`, ESM
    NodeNext `.js` imports, dry-run default, etc.) and `.planning/PROJECT.md` for full
    project context.
    ```

    Match the markdown content above EXACTLY (you may use a different list character or
    paragraph order ONLY where the content is identical in meaning, e.g. line wrapping, but
    every fenced code block, every section header, every literal command, and every variable
    name MUST appear verbatim).
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; [ "$(wc -l &lt; README.md)" -ge 80 ] &amp;&amp; grep -q "^# BambooHR Candidate Screener" README.md &amp;&amp; grep -q "^## Cron Setup" README.md &amp;&amp; grep -q "^### macOS" README.md &amp;&amp; grep -q "^### Linux server" README.md &amp;&amp; grep -q "^## Compliance" README.md &amp;&amp; grep -q "crontab -e" README.md &amp;&amp; grep -q -- "--env-file /etc/screener.env" README.md &amp;&amp; grep -q -- "-v /etc/screener-config.yaml:/app/config.yaml:ro" README.md &amp;&amp; grep -q "bamboohr-screener:latest" README.md &amp;&amp; grep -q "0 8 \* \* \*" README.md &amp;&amp; grep -q "BAMBOOHR_API_KEY" README.md &amp;&amp; grep -q "BAMBOOHR_SUBDOMAIN" README.md &amp;&amp; grep -q "OPENAI_API_KEY" README.md &amp;&amp; grep -q "LIVE_MODE" README.md &amp;&amp; grep -q "CONFIG_PATH" README.md &amp;&amp; grep -q "ATS settings access" README.md &amp;&amp; grep -q "Data Processing Agreement" README.md &amp;&amp; grep -q "{\"processed\"" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at the project root
    - `wc -l < README.md` returns a value &gt;= 80 (full README, not stub)
    - First line is `# BambooHR Candidate Screener` (top-level title)
    - Contains the section header `## Cron Setup` (exact match)
    - Contains the subsection header `### macOS` (exact match)
    - Contains the subsection header `### Linux server` (exact match)
    - Contains the section header `## Compliance` (exact match)
    - Contains the literal string `crontab -e`
    - Contains the literal string `--env-file /etc/screener.env`
    - Contains the literal string `-v /etc/screener-config.yaml:/app/config.yaml:ro`
    - Contains the literal string `bamboohr-screener:latest`
    - Contains the literal cron schedule `0 8 * * *`
    - Contains all five env var names: `BAMBOOHR_API_KEY`, `BAMBOOHR_SUBDOMAIN`, `OPENAI_API_KEY`, `LIVE_MODE`, `CONFIG_PATH` (each verifiable with grep)
    - Contains the literal phrase `ATS settings access` (BambooHR permission requirement)
    - Contains the literal phrase `Data Processing Agreement` (GDPR compliance note)
    - Contains a JSON summary example starting with `{"processed"` (INFRA-03 documentation)
    - Does NOT contain TODO markers — `grep -iE "TODO|TBD|FIXME|XXX" README.md` returns nothing
    - Does NOT use inline `-e KEY=value` syntax for secrets in any cron snippet (verifiable: `grep -E '^.*-e [A-Z_]+=' README.md` should return nothing OR only return lines that document this anti-pattern as a thing to AVOID)
  </acceptance_criteria>
  <done>`README.md` exists with all required sections (Quick Start, Build, Run, Configuration, Cron Setup with macOS + Linux, Operating Notes, Compliance), a copy-paste cron line, all five env var names documented, and the GDPR/ATS-permission compliance gates called out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → Operator | Documentation is the only mechanism that prevents the operator from misconfiguring the runtime |
| Cron entry → Shell history / log files | The exact `docker run` line documented here is what gets stored in shell history and cron logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Information Disclosure | Operator copies an inline `-e BAMBOOHR_API_KEY=...` example from the README, baking the secret into shell history and cron logs | mitigate | The README documents `--env-file /etc/screener.env` exclusively (D-06). No `-e SECRET=value` patterns appear anywhere in the README. The Operating Notes section explicitly warns against the inline-flag anti-pattern. |
| T-04-16 | Tampering | Operator follows out-of-date instructions (e.g. legacy BambooHR base URL, old image name) and the deployment fails | mitigate | The README is generated alongside the Dockerfile in this same phase, so version skew at v1 is impossible. The image name (`bamboohr-screener:latest`) matches Plan 02 exactly. |
| T-04-17 | Elevation of Privilege | Operator enables `LIVE_MODE=true` without the GDPR DPA / consent disclosure in place — exposes the company to legal liability | mitigate | The Compliance section calls out both pre-requisites in a blockquote at the bottom of the README and again in the Quick Start ("see Compliance below first"). REQUIREMENTS.md compliance note is mirrored verbatim. |
| T-04-18 | Repudiation | A failed cron run leaves no audit trail | mitigate | The README requires `>> /var/log/screener.log 2>&1` in the cron entry. Operating Notes explains how to detect failures by parsing the final JSON summary line. |
| T-04-19 | Information Disclosure | `/etc/screener.env` permissions are too open and a low-privilege user reads the API keys | mitigate | The README explicitly documents `chmod 600 /etc/screener.env` and ownership requirements. |

</threat_model>

<verification>
- All grep-based acceptance criteria pass
- README is at least 80 lines long
- Every required section header is present (Quick Start, Build, Run, Configuration, Cron Setup with macOS + Linux subsections, Operating Notes, Compliance)
- The cron snippet is copy-paste-ready (uses absolute paths, --env-file, -v mount, --rm, and log redirection)
</verification>

<success_criteria>
- `README.md` exists and is the canonical operator documentation for the screener
- INFRA-04 is satisfied: copy-paste-ready macOS crontab entry + Linux server deployment note are both present
- The README documents the dry-run default and the LIVE_MODE opt-in path
- The README documents the GDPR DPA + candidate consent requirements before LIVE_MODE
- The README documents the ATS settings access requirement on the BambooHR API key
- The README documents how to read the INFRA-03 summary line from cron logs to detect successful runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-live-mode-deployment/04-03-SUMMARY.md` documenting:
- Confirmation that README.md exists with all required sections
- The exact crontab line shipped (so a future reviewer can diff it against operator deployments)
- Confirmation that INFRA-04 is satisfied
- Any operator-facing notes that were added beyond the planned content
</output>
